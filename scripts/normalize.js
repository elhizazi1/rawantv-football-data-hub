// Normalization currently happens inside mergers; this entrypoint exists for
// future heavy normalization passes (e.g. cross-source player photo dedupe).
console.log("Normalization is embedded in mergers/. Run `npm run merge`.");
