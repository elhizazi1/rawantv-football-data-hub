{
  "name": "rawantv-football-data-hub",
  "version": "1.0.0",
  "description": "Unified football data warehouse for the RawanTV mobile app.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "collect": "node scripts/collect.js",
    "normalize": "node scripts/normalize.js",
    "merge": "node scripts/merge.js",
    "build": "node scripts/build.js",
    "validate": "node scripts/validate.js",
    "api": "node api/server.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "node-fetch": "^3.3.2",
    "slugify": "^1.6.6"
  }
}
